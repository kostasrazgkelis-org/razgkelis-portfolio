@page "/not-found"
@layout MainLayout

<PageTitle>Not found — Konstantinos A. Razgkelis</PageTitle>

<section class="section" style="padding-top:calc(var(--nav-h) + 6rem);min-height:80svh;display:flex;align-items:center">
    <div class="container contact-cta">
        <div class="display" style="font-size:clamp(5rem,20vw,12rem);color:var(--accent)">404</div>
        <h2 style="margin-top:1rem">Page not found</h2>
        <p class="lead" style="margin:1rem auto 1.6rem;max-width:440px">
            The page you're looking for doesn't exist or has moved.
        </p>
        <a class="btn btn--primary" href="">Back home</a>
    </div>
</section>
